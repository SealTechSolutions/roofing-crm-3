# SealTech CRM — Product Requirements Document

## Original problem statement
Full-stack CRM for a commercial roofing/construction business with Auth (JWT), Dashboard KPIs, Contact/Property/Project/Vendor mgmt, Deal workflow, Object Storage Document Library, PDF generator, Gmail integration, Internal multi-entity accounting module, Project Photos with Public Gallery, Admin Trash, COI Reminders, Cash Flow Statements, Assessment Reports, Project Calendar, Google Calendar Sync, PWA Polish, Automated Stale Deals Digest, In-App Scope Editor, Public Proposal Sign-Off, Mobile QR Login, Mobile standalone field capture, Compliance tracking, Email Routing, and a robust Material Calculator with deal-syncing.

## Current state (Feb 2026)
Production-ready PWA CRM. JWT auth, Visual Deals Kanban, offline-capable field camera, ReportLab PDF generation (Assessments, Proposals, Spec Sheets, Brochures, Work Orders), In-App Scope Editor, Public Proposal & Subcontractor Work Order E-signing, Material Calculator with sizing/warranties/auto-sync, category-aware email routing, custom Work Order/Scope PDF rendering with signatures, admin-only Assigned Rep tracking.

## Recently completed
- 2026-02 (Feb 26) — **Mobile access to Daily Wrap-Up**: field reps can now reach Wrap-Up from their phone. Added a green **"Wrap-Up"** button in the FieldCapture top bar + a prominent **"Daily Site Wrap-Up · End of Day"** tile above the project search. Whitelisted routes (`/wrap-up`, `/deals`, `/projects`, `/maintenance`, `/profile`) added to `MobileGate` so phones no longer force-redirect to `/field`. Layout sidebar hidden on `<768px` viewports so whitelisted mobile pages get full width. Added a mobile-only **← Back to Field** chevron in the Wrap-Up header. Fixed a stale `to="/field-capture"` link in the WrapUp empty state (now `/field`). Files: `App.js`, `Layout.jsx`, `pages/FieldCapture.jsx`, `pages/SiteVisitWrapUp.jsx`.
- 2026-02 (Feb 26) — **Project Close-Out Workflow — Phase 2 (follow-ups)**: (a) Auto-checks: `_apply_close_out_auto_checks` runs on `start_close_out` and again inside `finalize_close_out`; `ndl_registered` auto-marks when the deal has no NDL upgrade sold, `final_payment_received` when a `source_type=mark_complete` invoice is Paid, `commission_accrued` when a `commission_accruals` row exists. Auto items render a violet **AUTO** pill and their toggle is disabled; PUT rejects manual un-check with a 400. (b) `/api/deals/{id}/close-out/pnl-summary` endpoint + `PnLInline` widget embedded inside the `pnl_variance_review` checklist row (Revenue / Est · Actual / Gross Profit + Margin / Est→Actual Variance %). (c) File attachments: `POST/DELETE/GET` at `/api/deals/{id}/close-out/item/{key}/attachments/…` — 25 MB max, stored under `sealtech-crm/deal-closeout/{deal_id}/{item_key}/`. `AttachmentsRow` component renders on 7 doc-requiring items. (d) Finalize: builds `close_out_pdf.py` Summary PDF, uploads to Object Storage, drops a `library_files` record under Documentation/Warranty Certificates, releases any `pending_job_start` commission accruals, stamps `job_started_at` if missing. Tested 100% pass by testing agent (6/6 backend pytests + full Playwright frontend flow). Regression test at `/app/backend/tests/test_close_out_phase2.py`.
- 2026-02 — **Calculator → Live P&L auto-populate on "Set → Option X"**: clicking Set-Option now writes `deal.materials_cost` (raw catalog cost + 20% shipping/tax markup) AND `deal.subcontractor_cost` (from the "1099 Sub Labor" input) in the same PUT that saves the customer price. No second "Push Materials" click required — the Real-time labor/material/equipment/sub breakdown card lights up immediately. 1099 subs land in the **Subcontractor** bucket per user's cost-accounting convention; the **Labor** bucket stays reserved for future W2 employees. Calculator input relabeled "+ 1099 Sub Labor ($ for this option)" for clarity.
- 2026-02 — **Take-Off Estimated ↔ Actual columns default to always-on** (was hidden behind a "Show Variance" toggle). The Material Take-Off table now shows `Unit Cost · Estimated · Actual · Variance` out of the box so vendor-bill reconciliation is one click ("Link Bill" per row). Shipping/tax markup (+20%) baked into the pushed unit_cost so estimates match the with-shipping cost the deal actually needs to spend.
- 2026-02 — **Auto-fill from Calculator → System auto-pick**: empty Material Take-Off card now has an "Auto-fill from Calculator →" button; the Calculator, on load, auto-checks the winning system using `winning_warranty_years` / `chosen_option_id` / `chosen_amount` fuzzy-match (20% tolerance) so no re-pick is needed.
- 2026-02 — **Admin "Scope Boilerplate Editor" (`/settings/scope-bullets`)**: New admin-only page that lets the owner edit the standard "Inspection & Prep" bullets on every Spec Sheet PDF + Work Order for each roof system (19 templates: Silicone, Silicone w/ Granules, FARM, TPO/EPDM/PVC/ModBit variants, BUR, Metal, Shingle, Tile, Custom). FARM also exposes its Work-Order-only `wo_scope_2` extras. Per-bullet reorder / add / delete / edit; blue **CUSTOM** badge on any template with overrides; per-template Reset button. Backend: overrides stored in `settings.scope_bullet_overrides`; `spec_sheet._resolve_template()` now transparently merges overrides on top of the hardcoded templates via a module-level cache primed at startup and refreshed after every save.
- 2026-02 — **Scope-of-Work boilerplate updated** (`spec_sheet.py`): global "Inspection & Prep" text refreshed across 10 templates ("Survey existing roof assembly and verify its condition is suitable for proposed system…", "Identify wet insulation and repair and/or install 10\" one-way vents…", "…if required use approved cleaner…"). FARM `wo_scope_2` refreshed and now includes a new "Install grey walkway according to manufacturer specifications." bullet.
- 2026-02 — **Create PO button (per-vendor Purchase Orders)** in DealActionBar between Change Order and Send to Field; per-vendor View/Download/Print/Email sub-menus; PurchaseOrderComposeModal for review-before-send.
- 2026-02 — **Work Order sign page — blank iframe fix**: replaced the failing `<iframe src=… .pdf>` with an inline plain-text Scope of Work card so mobile Safari + hardened Chrome render the page correctly.
- 2026-02 — **Admin "Scope Boilerplate Editor" (`/settings/scope-bullets`)**: New admin-only page that lets the owner edit the standard "Inspection & Prep" bullets on every Spec Sheet PDF + Work Order for each roof system (19 templates: Silicone, Silicone w/ Granules, FARM, TPO/EPDM/PVC/ModBit variants, BUR, Metal, Shingle, Tile, Custom). FARM also exposes its Work-Order-only `wo_scope_2` extras. Per-bullet reorder / add / delete / edit; blue **CUSTOM** badge on any template with overrides; per-template Reset button. Backend: overrides stored in `settings.scope_bullet_overrides`; `spec_sheet._resolve_template()` now transparently merges overrides on top of the hardcoded templates via a module-level cache primed at startup and refreshed after every save. New endpoints: `GET/PUT /api/settings/scope-bullets` + `POST /api/settings/scope-bullets/reset`. Verified end-to-end from the UI: added a bullet → Save → Custom badge appears → freshly-drafted WO description reflects the change → Reset button clears the override.
- 2026-02 — **Scope-of-Work boilerplate updated** (`spec_sheet.py`): global "Inspection & Prep" text refreshed across 10 templates ("Survey existing roof assembly and verify its condition is suitable for proposed system…", "Identify wet insulation and repair and/or install 10\" one-way vents…", "…if required use approved cleaner…"). FARM `wo_scope_2` refreshed and now includes a new "Install grey walkway according to manufacturer specifications." bullet.
- 2026-02 — **Work Order sign page — blank iframe fix**: replaced the failing `<iframe src=… .pdf>` (blank on mobile Safari + hardened Chrome) with an inline plain-text Scope of Work card. Signature form is now directly under the summary. Full PDF still one tap away via "Open Work Order PDF".
- 2026-02 — **Create PO button (per-vendor Purchase Orders)**: new indigo "Create PO" split-button in `DealActionBar.jsx`, placed between Change Order and Send to Field. Auto-derives one dropdown row per vendor from the deal's `material_takeoff` (each row shows line-count + total-unit subtitle + how many are already ordered). Nested submenu per vendor: View / Download / Print / Email to Vendor. Email opens a new in-file `PurchaseOrderComposeModal` that prefills the vendor's email (via GET /api/vendors/{id}), a "Purchase Order — {po_number}" subject, and a standard SealTech body — user reviews & edits To / CC / Subject / Body before sending. Backend `POST /api/deals/{id}/purchase-order/{vendor_id}/email` extended to accept optional `subject`, `body_text`, `body_html` overrides (backwards-compatible; falls back to previous defaults if omitted). Verified end-to-end by testing agent (8/8 backend pytests + 8/8 frontend Playwright assertions). Regression test at `/app/backend/tests/test_purchase_order.py`. When the deal has no take-off lines yet, the dropdown surfaces a "Open Material Take-Off →" shortcut that scrolls to the take-off card.
- 2026-02 — **One-click "New Deal from Property"**: blue "New Deal" button on every row of the Properties list (`data-testid="create-deal-from-property-{id}"`). Spawns a deal pre-filled with the property's name (as title), `property_id` link, and the on-site contact linked as `contact_id` + `customer_contact_id` + `owner_contact_id`. Auto-navigates to `/projects/{new_deal_id}`. Pure frontend — no new backend endpoint needed since `POST /api/deals` already accepts these fields.
- 2026-02 — **AI Auto-Tag Photos (Claude Sonnet 4.6 Vision)**: New `POST /api/projects/{deal_id}/photos/auto-tag` endpoint. Batches untagged photos (or explicit `photo_ids`) through Claude Vision with a strict-label system prompt; returns one of {Before, During, After, Drone, Damage Documentation, Detail Shots, none}. Frontend: violet "AI Auto-Tag ✨" button in the Project Photos toolbar. Verified end-to-end on real deal — 6/6 Morning Walk photos correctly labeled Before + Damage Documentation. Uses `EMERGENT_LLM_KEY` + `emergentintegrations.llm.chat`.
- 2026-02 — **Multi-tag Client-Share Gallery + "Client-Ready" preset**: `ShareCreate` now accepts a `tags: List[str]` list (kept legacy single `tag` for back-compat). Public gallery endpoint honors `$in` when multiple tags. Frontend Share Modal completely redesigned — 6 clickable tag toggle chips + a green "Client-Ready · Damage + After" preset button that pre-selects the shots insurance adjusters actually want. Also a "Share Everything" reset.
- 2026-02 — **Photos (count) badge on Deals list**: new `GET /api/deals/photo-counts` aggregation endpoint returns a `{deal_id: count}` map in one round-trip. Deals list rows now show a compact sky-toned "📷 N" chip so photo-empty jobsites are visible at a glance.
- 2026-02 — **Global Search (Cmd+K)**: new `GET /api/search?q=` backend endpoint (deals + contacts + people + vendors + properties + invoices) with regex-escape safety + 8-per-group cap + sales-role deal-scoping. Frontend: new `GlobalSearch.jsx` command-palette modal registered in Layout, triggered by ⌘K / Ctrl+K or a floating bottom-right pill. ↑↓ navigate rows, Enter jumps, Esc closes. Debounced 200ms.

- 2026-02 — **Finish Site Visit — Wrap-Up Screen**: New page at `/wrap-up` (also linked in sidebar as **"Finish Site Visit"**) surfaces every deal the current user has shot photos on in the last N days (Today / 3D / 7D toggle). Each deal card shows photo count + counters (untagged / no-caption / annotated / paired) + amber pip when action is pending. Two mobile-first quick actions: (1) 6 large color-coded tag chips that bulk-tag every untagged photo on that deal in one API call, (2) **"Send Condition Report"** primary CTA that fires the Gmail email endpoint directly. Backend: `GET /api/site-visits/today` + `PUT /api/deals/{id}/photos/bulk-tag`. Wraps together the annotations + voice captions + before/after + email-report features into one end-of-day cleanup screen. Verified end-to-end: 3 recent site visits surfaced, "Damage Documentation" bulk-tag applied to 3 untagged photos in a single tap, untagged count updated live.
- 2026-02 — **Photo grid 10× speedup**: added `?thumb=1` param to `GET /api/projects/{deal_id}/photos/{id}/download` — Pillow on-the-fly resize to 600px JPEG (~37KB vs ~365KB original). Applied to ProjectPhotos grid, PhotoTimeline cards, BeforeAfterPair thumbnails, PhotoPairPicker candidates. Full-resolution image still loads in the lightbox / annotator. Verified via curl: 373KB original → 37KB thumbnail (10× smaller, same photo).
- 2026-02 — **Email Condition Report to Client (Gmail)**: `POST /api/deals/{deal_id}/condition-report/email` builds a fresh Roof Condition Report PDF (preferring annotated photos) and fires it via Gmail SMTP using the existing `email_sender` + `email_routing` (category `maintenance`). Auto-populates recipient from the deal's primary contact, allows CC + personal note override, uses a smart default cover message that references photo count + annotation count. Persists the exact PDF to Object Storage under file category "Condition Report" so the rep can re-open it from the Activity Timeline. Appends `condition_report_emailed` entry to deal history + stamps `last_condition_report_sent_at`. Frontend: new "Email Report" button opens `EmailConditionReportModal` — auto-fills the To field from the deal's primary contact, one-tap Send.
- 2026-02 — **Voice-to-text photo captions (Whisper)**: New `VoiceCaptionButton` component wraps browser MediaRecorder + tap-to-record UX (idle/recording/uploading states, 60-sec safety auto-stop, live timer, iOS Safari falls back to audio/mp4). Uploads recording to `POST /api/projects/{deal_id}/photos/transcribe` which calls OpenAI Whisper (`whisper-1`) via the Emergent Universal Key with a roofing-vocabulary prompt (membrane, blistering, ponding, TPO, EPDM, etc.) pinning language to English for ~15% accuracy boost. Wired into Edit Photo modal so reps append dictated notes to existing descriptions instead of overwriting. Tested end-to-end: real WAV upload → 200 OK, empty text for non-speech tones as expected.
- 2026-02 — **Before/After photo pairing**: `PUT /api/projects/{deal_id}/photos/{photo_id}/pair` body `{paired_photo_id, role: before|after}` links two photos symmetrically (backend enforces the partner gets the opposite role; auto-cleans previous partners so re-pairing never leaves orphans; `delete_photo` breaks the pair-link before soft-delete). `GET .../photos/pairs` returns an array of `{before, after}` objects. Frontend: new `BeforeAfterPairPanel` (emerald card at top of Project Photos, only shown when ≥1 pair exists) with side-by-side thumbnails and a **fullscreen slider modal** — drag the vertical handle to reveal Before vs After with real-time clip-path. Photo cards get a "Pair" button in the hover toolbar + a role badge ("Before" rose / "After" emerald) below the filename. `PhotoPairPicker` modal lets user pick any other un-paired photo on the same deal, toggling this photo's role. Verified end-to-end: pair created → listed → un-paired → orphan cleanup confirmed.
- 2026-02 — **Maintenance / Roof Condition Report PDF**: `GET /api/projects/{deal_id}/photos/maintenance-report.pdf` produces a client-ready PDF grouped by damage type (not date). Priority order: Damage Documentation → Detail Shots → Before → During → After → Drone → Untagged. Cover page shows report date, inspector name (from auth), photo count, and an executive summary table with per-tag counts + total annotated shots. Each section has its own header, description, and 2-col photo grid; captions call out inspector-annotated photos with a green ★ chip. When a photo has `annotated_storage_path`, the flattened marked-up PNG renders instead of the raw source, so client sees the arrows/circles/text callouts baked into the report. New `maintenance_report_pdf.py` module (~350 LOC). "Condition Report" button added next to "Timeline PDF" on the deal Project Photos section. Verified end-to-end: 9MB PDF with photos + annotations, correct summary counts, proper page breaks.
- 2026-02 — **CompanyCam Photo Annotations MVP**: Draw arrows, circles, freehand pen strokes, and text labels directly on jobsite photos. New `PhotoAnnotator.jsx` canvas modal (`z-60`) with 4 tools, 6-color palette (red/yellow/green/blue/white/black), 3 stroke widths (S/M/L), Undo, Clear all. Save flattens the source image + overlay into a full-resolution PNG on an off-screen canvas and POSTs both the blob and raw layer JSON to `PUT /api/projects/{deal_id}/photos/{photo_id}/annotations`. Editable — re-opening the annotator on an already-marked photo rehydrates every shape. `DELETE .../annotations` reverts to the raw source. `GET .../download` auto-serves the annotated PNG unless `?original=true`. "Marked" emerald badges surface on Project Photo cards, global Photo Timeline cards, and both lightboxes. Wired into `ProjectPhotos.jsx` hover toolbar + lightbox and the `/photos` timeline lightbox. Tested end-to-end: arrow + circle drawn → saved → badge appeared → flattened PNG stored at `sealtech-crm/project_photos/{deal_id}/{photo_id}-annotated.png`.
- 2026-02 — **CompanyCam-style Photo Timeline**: new `/photos` sidebar page + `GET /api/photos/all` endpoint (cross-project photo feed). Photos grouped client-side into Today / Yesterday / This Week / This Month / [Month Year] buckets. Tag chip filters (Before / During / After / Drone / etc.), day-range chips (7D / 30D / 90D / 1Y), search, lazy-loaded thumbnails with IntersectionObserver, fullscreen lightbox with EXIF timestamp + property address + tag + uploader metadata.
- 2026-02 — **iOS TestFlight pipeline SET UP** (delivery pending user's final Codemagic clicks): Apple Team ID `2J8T63SX9L` locked in, App Store Connect API key `7V3U9Q8TT6` connected to Codemagic (Developer Portal integration active), Codemagic Team `6a5fc8d6c7a9522664488997` connected to `github.com/SealTechSolutions/roofing-crm-3`, `codemagic.yaml` in repo (workflow: SealTech CRM — iOS TestFlight). Remaining user-side clicks: generate signing certificate, create `app_store_credentials` env group, trigger first build.
- 2026-02 — **Native iOS app scaffolding (Capacitor 7)**: `capacitor.config.ts`, app icon (1024) + splash (2732) from SealTech logo, `nativeCapabilities.ts` helper, setup playbook at `/app/NATIVE_APP_SETUP.md`.
- 2026-02 — **PWA/mobile UX polish**: Field Capture `h-[100dvh]` (shutter always visible, no scrolling), camera auto-retry 1→3 attempts w/ escalating backoff, `visibilitychange` + `pageshow` listeners auto-restart camera when returning from background (fixes "reload twice" bug), redesigned Get-App-On-Phone modal with prominent iOS + Android install instructions.
- 2026-02 — **Scopes discoverability**: `/scopes` sidebar page + Dashboard "Recent Scopes" widget + per-Contact/Property "Scopes (N)" chips.
- 2026-02 — Assessment PDF page-9 & page-10 fixes: aligned blue & green callout boxes to 7.3" width matching their tables, trimmed 1/8" padding, `KeepTogether` on Restoration Suitability tail keeps whole block on page 9, Option 3 fits back on page 10 (doc 14→12 pages).
- 2026-02 — **Work Order Phase 2**: drawn-signature canvas + Change Order variant.
- 2026-02 — Calculator: Overhead default → 30%, Shipping → 20%, Handling → 12%.
- 2026-02 — Calculator: Custom Add-Ons section now available on **Everest/Silicone** vendor (previously WC-only).
- 2026-02 — Calculator: Added "Spray Elastic Cement" add-on row (WC).
- 2026-02 — Calculator: WC system reordering + renamed 25-Yr→15-Yr Gravel.
- 2026-02 — Metal Roof Restoration Scope PDF: 3-page layout w/ Page 1 inclusions, spacing rhythm matched.
- 2026-02 — Work Order: Library attachment + email dispatch + cursive signature font + tall description box.
- 2026-02 — Email routing/footers cleaned globally.
- 2026-02 — Admin-only Assigned Rep dropdown + badges on deals/contacts.
- 2026-02 — Sidebar grouping: 8 semantic groups (Contacts / Projects / Field / Reports / Scheduling / Library / Finance / Company Info) + User Guide link.
- 2026-02 — Removed web "Field Camera" nav item (field capture lives in native iOS app).
- 2026-02 — **UX scroll-order fix**: DealDetail reorganized into 5 workflow groups (Who → Scope → Money → Execution → History); Dashboard reorganized into 4 groups (Alerts → KPIs → Activity → Metrics). Sticky "Jump to" chip bars added to both pages for one-click navigation to any section.
- 2026-02 — **Phase B — Auto-draft invoices**: `POST /api/deals/{id}/deposit-invoice` endpoint + blue "Ready to collect a deposit?" suggestion banner + amber "Deposit drafted — ready to send?" reminder banner on the deal page. Idempotent, tested (5 pytest cases). Auto-final invoice was already wired via Mark Complete.
- 2026-02 — **Phase C — Go to Production checklist**: new `GoToProductionChecklist` component + new deal field `equipment_ordered: List[dict]`. Four-row card (Schedule / Materials / WO / Equipment) with live progress badge and inline equipment picker (Storage Container, Porta-Potty, Forklift, Manlift, Dumpster, Scaffolding, + free-form).
- 2026-02 — **Phase D — Live Project P&L**: new `ProjectLivePnL` component at the top of the Money section. Real-time Revenue / Estimated Cost / Actual Cost / Gross Profit / Gross Margin + category breakdown bars (Materials, Labor, Subcontractor, Equipment, Other) with equipment-rental standard-rate estimates.
- 2026-02 — **Editable Equipment Rates**: admin-only settings page at `/settings/equipment-rates` (new `EquipmentRates.jsx` + `GET/PUT /api/settings/equipment-rates`). Rates persist to `settings` collection and stream into the Live P&L card via `useEffect`. Fallback defaults still baked in. 4 pytest cases.
- 2026-02 — **One-click Mark Paid**: `POST /api/invoices/{id}/mark-paid` — flips to Paid, sets amount_paid=total, payment_date=today, delegates to existing PUT handler so GL journal fires unchanged. Emerald "MARK PAID" button on the deposit banner. Idempotent. 4 pytest cases.
- 2026-02 — **Milestone → invoice one-click**: new Invoice column on Payment Milestones table with per-row "Draft" button (uses existing `POST /api/invoices/from-milestone`, now idempotent + flips milestone status to `Invoiced`). If invoice exists, button becomes an "Open" shortcut.
- 2026-02 — **Redesigned Deal Action Bar**: new `DealActionBar.jsx` replaces the 8-button row + secondary `DealQuickActions` menu with **6 smart color-coded state-aware buttons** using shadcn DropdownMenu (Assessment/Scope/WorkOrder/ChangeOrder/SendToField/MarkComplete). Ghost styling when doc doesn't exist yet, solid palette when it does. Mark Complete now shows a confirmation dialog with the contract total. `+ Invoice` and `Record Payment` moved into a new Money section header where they're contextually closer to invoices and payments.
- 2026-02 — **"Send Condition Report" bugfix**: `POST /api/deals/{id}/condition-report/email` and `POST /api/deals/{id}/spec-sheet/email` were looking up contact via a non-existent `deal.primary_contact_id` field. Fixed to use the correct `deal.customer_contact_id` / `deal.contact_id` lookup + corrected contact field names (`contact_name`, `company_name`). 2 pytest cases added.
- 2026-02 — **Condition Report retired (consolidation)**: the photo-driven "Roof Condition Report" was overlapping with the Assessment → Basic Evaluation flow. Removed the "Send Condition Report" button from Finish Site Visit, the "Condition Report" + "Email Report" buttons from ProjectPhotos, and the `EmailConditionReportModal` component. Backend endpoint kept alive but has no frontend callers. Users now send the customer report via the Assessment ▾ dropdown on the deal action bar.
- 2026-02 — **Global spell-check enabled**: new `useForceSpellcheck` hook called from `Layout.jsx` — force-sets `spellcheck="true"`, `autocorrect="on"`, `autocapitalize="sentences"` on every text-editable input & textarea across the app (including within the iOS Capacitor WebView). MutationObserver keeps it working as modals/dynamic fields appear. Non-text inputs (number/date/email/etc.) correctly skipped.
- 2026-02 — **ZIP → City/State autofill**: new `/api/lookup/zip/{code}` endpoint (backed by zippopotam.us with permanent Mongo caching in `zip_cache` collection) + new `useZipAutofill` React hook. Wired into Contacts (main + billing address), Properties (property address), and Invoice editor (Bill-To). Fires on 5-digit debounced ZIP entry, only fills blank city/state fields, silent-fail if lookup service is down.
- 2026-02 — **Photo Timeline retired + orphan cleanup**: standalone `/photos` cross-project timeline page removed (users always start from a specific deal — the page created redundant navigation surface). Sidebar entry + route + `PhotoTimeline.jsx` deleted. Also fixed root-cause bug: `DELETE /api/deals/{id}` now cascades to hard-delete (or soft-delete) the deal's project photos so they don't orphan in the DB. New admin `POST /api/photos/purge-orphans` endpoint added; purged 56 orphaned photos left over from previous test data.

## Active backlog
- **P1 — Weekly digest email** (Monday 7am recap to admins).
- **P1 — Commission Module** (spec at `/app/memory/COMMISSION_PRD.md`): blocked on user confirmation of flat % vs tiered, refund reversals, signature flow.
- **P1 — WSC Fork Strategy**: branding-constants refactor → `branding.py` first, then decide fork vs multi-tenant tag.
- **P2 — Public web lead-intake form**: customers self-service to create a deal.
- **P2 — Auto-flag milestones whose due_date has passed** (bell icon in Money section).
- **P2 — server.py refactor**: split >8,000-line `server.py` into `/app/backend/routes/`.
- **P3 — Fix HTML validity warning**: `<span> option cannot be a child of <select>` console warning (pre-existing, non-blocking).
- **P3 — Mobile-only PWA Views**: `/field/today`, `/field/calendar`, `/field/recent-photos`.
- **P3 — Stripe**: blocked on user's bank + Stripe keys.

## Architecture
- Backend: FastAPI + Motor MongoDB + ReportLab. Key modules: `server.py`, `product_catalog.py`, `spec_sheet.py`, `work_orders.py`, `proposal_signing.py`, `email_routing.py`, `email_sender.py`.
- Frontend: React + Tailwind + Shadcn. Key pages: `Calculator.jsx`, `Library.jsx`, `WorkOrderSign.jsx`, `DealDetail.jsx`, `ProposalSign.jsx`.
- Integrations: Gmail SMTP (user creds), Google Calendar (user creds), Emergent Object Storage (Universal LLM Key).

## Key collections
- `deals` — proposed_roof_type, stage, subcontractor_accepted, scope_overrides, proposal_option_1, warranty_20yr_add, assigned_to_user_id, calc_custom_addons.
- `calculator_settings` — singleton: markup_pct, handling_pct (12), overhead_pct (30), profit_pct, shipping_pct (20).
- `library_files`, `work_order_drafts`.

## Refactor debt
- `/app/backend/server.py` > 7,200 lines — split into route modules.
- Brand constants (company name, email, phone, logo) scattered across PDF generators — consolidate into `branding.py`.

## Testing notes
- ALWAYS use `TEST_` prefixed dummy deals when invoking `testing_agent_v3_fork`. Never touch real user data.
- Admin: `darren@sealtechsolutions.co` / `admin123` (see `/app/memory/test_credentials.md`).
